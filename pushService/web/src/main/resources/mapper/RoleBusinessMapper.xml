<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qcby.personalmanagement.web.mapper.RoleBusinessMapper">

    <!--定义接口方法对应的 SQL 语句-->

    <delete id="deleteByRoleId" parameterType="java.lang.Long">
        delete from sys_role_business
        where role_id = #{id};
    </delete>

    <delete id="bactchDeleteByRoleIds" parameterType="java.util.List">
        DELETE FROM sys_role_business
        WHERE role_id IN
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

<!--    &lt;!&ndash; 定义插入语句 &ndash;&gt;-->
<!--    <insert id="insertBatch">-->
<!--        INSERT INTO sys_role_business (role_id, business_id)-->
<!--        VALUES-->
<!--        <foreach collection="list" item="item" separator=",">-->
<!--            (#{item.column1}, #{item.column2})-->
<!--        </foreach>-->
<!--    </insert>-->

</mapper>
